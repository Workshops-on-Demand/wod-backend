---
- name: Setup Playbook for WKSHP-Spark101 appliance
  hosts: "{{ WODRTARGET | default([]) }}"
  gather_facts: true
  tasks:
    - name: Setup docker based appliance
      ansible.builtin.include_tasks: "{{ WODANSIBLEDIR }}/setup_docker_based_appliance.yml"
