#!/bin/bash

set -x

{{ WODSCRIPTDIR }}/reset-appliance.sh

# Cleanup
ssh -q {{ NOCHECKSSH }} $WODRTARGET sudo su - student$stdid -c \'rm -rf {{ STUDDIR }}/student$stdid/.??* {{ STUDDIR }}/student$stdid/*\'

exit 0
